generator client {
  provider        = "prisma-client-js"
  output          = "../generated/prisma"
  previewFeatures = ["relationJoins"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model botConfig {
  id              BigInt       @id @default(autoincrement())
  awardType       BigInt
  xpPerAward      Float        @db.Real
  created_at      DateTime     @default(now()) @db.Timestamptz(6)
  awardMessage    String?
  cooldown        BigInt?
  awardChannel    String?
  whitelistmode   Boolean      @default(false)
  donationChannel String       @default("")
  honeypotChannel String?      @default("")
  xpAwardTypes    xpAwardTypes @relation(fields: [awardType], references: [id], onDelete: Cascade)
}

model channelBlacklist {
  id          BigInt   @id @default(autoincrement())
  channelId   String   @unique(map: "channelBlacklist_channeldId_key")
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  blacklisted Boolean  @default(true)
}

model channelConfig {
  id            BigInt   @id @default(autoincrement())
  channelId     String   @unique
  followThreads Boolean  @default(true)
  created_at    DateTime @default(now()) @db.Timestamptz(6)
}

model ignoredCharacters {
  id          BigInt   @id @default(autoincrement())
  ignoredChar String   @unique
  created_at  DateTime @default(now()) @db.Timestamptz(6)
  ignored     Boolean? @default(true)
}

model thresholds {
  id         BigInt   @id @default(autoincrement())
  tier       Int      @unique
  xpRequired BigInt
  xpGiven    BigInt
  created_at DateTime @default(now()) @db.Timestamptz(6)
  userXp     userXp[]
}

model xpAwardTypes {
  id         BigInt      @id @default(autoincrement())
  awardType  String
  created_at DateTime    @default(now()) @db.Timestamptz(6)
  botConfig  botConfig[]
}

model userXp {
  id           BigInt      @id @default(autoincrement())
  userId       BigInt      @unique
  xp           Float
  created_at   DateTime    @default(now()) @db.Timestamptz(6)
  last_message DateTime    @db.Timestamptz(6)
  rank         Int?
  thresholds   thresholds? @relation(fields: [rank], references: [tier], onDelete: NoAction, onUpdate: NoAction)
  players      players     @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "userxp_userid_fkey")
}

model xpDonationEntities {
  id              BigInt          @id @default(autoincrement())
  entityName      String
  created_at      DateTime        @default(now()) @db.Timestamptz(6)
  totalXp         Int             @default(0)
  donationMessage String          @default("\"\"")
  entityImageUrl  String          @default("")
  shortName       String?
  xpDonationLog   xpDonationLog[]
}

model xpDonationLog {
  id                 BigInt             @id @default(autoincrement())
  userId             BigInt
  xpDonated          BigInt             @default(0)
  entityId           BigInt
  messageLink        String
  created_at         DateTime           @default(now()) @db.Timestamptz(6)
  character          BigInt?
  characters         characters?        @relation(fields: [character], references: [id], onDelete: Cascade, onUpdate: NoAction)
  xpDonationEntities xpDonationEntities @relation(fields: [entityId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  players            players            @relation(fields: [userId], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

model webhookInfo {
  id           BigInt   @id @default(autoincrement())
  webhookId    String   @default("")
  webhookToken String   @default("")
  created_at   DateTime @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model characters {
  id            BigInt          @id @default(autoincrement())
  player        BigInt
  name          String
  created_at    DateTime        @default(now()) @db.Timestamptz(6)
  enabled       Boolean         @default(true)
  players       players         @relation(fields: [player], references: [id], onDelete: Cascade, onUpdate: NoAction)
  xpDonationLog xpDonationLog[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model players {
  id            BigInt          @id @default(autoincrement())
  userId        String          @unique
  created_at    DateTime        @default(now()) @db.Timestamptz(6)
  characters    characters[]
  userXp        userXp?
  xpDonationLog xpDonationLog[]
}

model xpDonationThresholds {
  id         BigInt   @id @default(autoincrement())
  xpRequired Int
  tier       Int      @db.SmallInt
  created_at DateTime @default(now()) @db.Timestamptz(6)
}
